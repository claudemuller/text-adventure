'use strict';

function createBlockedPathMessages() {
  const blockedPathMessages = [];

  blockedPathMessages.push(`It's too dangerous to move that way.`);
  blockedPathMessages.push('A mysterious force holds you back.');
  blockedPathMessages.push('A tangle of thorns blocks your way.');
  blockedPathMessages.push(`You can't step over the dragon.`);
  blockedPathMessages.push('');
  blockedPathMessages.push('The gate locks shut.');
  blockedPathMessages.push('The river is too keep to cross.');
  blockedPathMessages.push('The trees are too thick to pass.');
  blockedPathMessages.push(`You're too scared to go that way.`);

  return blockedPathMessages;
}
